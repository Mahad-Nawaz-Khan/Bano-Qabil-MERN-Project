# Vite reads this at build time. Values are baked into the bundle, so nothing secret belongs here.

# Same-origin path served through the Vite dev proxy. Point it at the full API URL
# (https://api.example.com/api) only when the site and API are deployed separately.
VITE_API_URL=/api
